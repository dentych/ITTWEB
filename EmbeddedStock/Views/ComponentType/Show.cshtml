@model ComponentType

@{
    ViewData["title"] = "Show componentType";
}

<div class="row">
  <div class="col-xs-12 col-md-6">
    <h2>ComponentType</h2>
    <table class="table">
      <tr>
        <th>ID</th>
        <td>@Model.ComponentTypeId</td>
      </tr>
      <tr>
        <th>Name</th>
        <td>@Model.ComponentName</td>
      </tr>
      <tr>
        <th>Info</th>
        <td>@Model.ComponentInfo</td>
      </tr>
      <tr>
        <th>Location</th>
        <td>@Model.Location</td>
      </tr>
      <tr>
        <th>Status</th>
        <td>@Model.Status</td>
      </tr>
      <tr>
        <th>Datasheet</th>
        <td>@Model.Datasheet</td>
      </tr>
      <tr>
        <th>ImageURL</th>
        <td>@Model.ImageUrl</td>
      </tr>
      <tr>
        <th>Manufacturer</th>
        <td>@Model.Manufacturer</td>
      </tr>
      <tr>
        <th>WikiLink</th>
        <td>@Model.WikiLink</td>
      </tr>
      <tr>
        <th>Admin comment</th>
        <td>@Model.AdminComment</td>
      </tr>
      <tr>
        <th>Components</th>
        <td>
          <ul>
            @foreach (var component in Model.Components)
            {
                <li>
                  <a asp-controller="Component" asp-action="Show" asp-route-id="@component.ComponentId">
                    @component.ComponentNumber
                  </a>
                </li>
            }
          </ul>
        </td>
      </tr>
      <tr>
        <th>Categories this type is part of</th>
        <td>
          <ul>
            @foreach (var category in Model.CategoryComponentTypes)
            {
                <li>
                  <a asp-controller="Category" asp-action="Show" asp-route-id="@category.Category.CategoryId">
                    @category.Category.Name
                  </a>
                </li>
            }
          </ul>
        </td>
      </tr>
    </table>
    <a asp-controller="Component" asp-action="Create" asp-route-componentTypeId="@Model.ComponentTypeId">
      <button class="btn btn-primary">Add component</button>
    </a>
  </div>
</div>